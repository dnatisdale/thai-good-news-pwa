import {Link,NavLink} from 'react-router-dom';import LanguageSwitcher from './LanguageSwitcher';import ThemeToggle from './ThemeToggle';import FontSizeButton from './FontSizeButton';import {t} from '@/lib/i18n';export default function Header(){return(<header className='sticky top-0 z-50 bg-white/90 dark:bg-neutral-900/90 backdrop-blur border-b'><div className='max-w-5xl mx-auto px-4 py-3 flex items-center gap-4 justify-between'><Link to='/' className='font-bold text-xl' aria-label={t('app_title')}>TGN</Link><nav className='flex items-center gap-3'><NavLink to='/' className='btn btn-secondary' aria-label={t('home')}>{t('home')}</NavLink><NavLink to='/add' className='btn btn-primary' aria-label={t('add_link')}>+ {t('add_link')}</NavLink><NavLink to='/favorites' className='btn btn-secondary' aria-label={t('favorites')}>{t('favorites')}</NavLink><NavLink to='/settings' className='btn btn-secondary' aria-label={t('settings')}>{t('settings')}</NavLink><NavLink to='/about' className='btn btn-secondary' aria-label={t('about')}>{t('about')}</NavLink></nav><div className='flex items-center gap-2'><LanguageSwitcher/><ThemeToggle/><FontSizeButton/></div></div></header>)}
