import {t} from '@/lib/i18n';import {useFontSize} from '@/hooks/useFontSize';export default function FontSizeButton(){const{scale,setScale}=useFontSize();return(<div className='flex items-center gap-2' aria-label={t('font_size')}><button className='btn btn-secondary' onClick={()=>setScale(Math.max(80,scale-10))}>{t('decrease_font')}</button><button className='btn btn-secondary' onClick={()=>setScale(Math.min(160,scale+10))}>{t('increase_font')}</button></div>)}
