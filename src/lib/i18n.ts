import en from '@/i18n/en.json';import th from '@/i18n/th.json';import {storage} from './storage';export type Messages=typeof en;const TABLE:{[k in 'en'|'th']:Messages}={en,th};const KEY='lang';export const getLang=():'en'|'th'=>storage.get(KEY,'en');export const setLang=(l:'en'|'th')=>{document.documentElement.lang=l;storage.set(KEY,l)};export const t=(k:keyof Messages,l:'en'|'th'=getLang())=>TABLE[l][k]??(k as string);
