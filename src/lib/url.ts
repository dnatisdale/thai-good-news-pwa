const HTTPS=/^(https:\/\/)[\w.-]+(?:\.[\w\.-]+)+(?:[\/?#][^\s]*)?$/i;export function normalizeUrl(s:string){let u=s.trim();if(u.startsWith('http://'))u='https://'+u.slice(7);if(!/^https?:\/\//i.test(u))u='https://'+u;return u}export function isValidHttpsUrl(s:string){try{const n=normalizeUrl(s);const u=new URL(n);return u.protocol==='https:'&&HTTPS.test(n)}catch{return false}}export const sanitizeText=(s:string)=>s.replace(/[<>]/g,'');
